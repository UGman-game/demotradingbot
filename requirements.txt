streamlit>=1.32
pandas>=2.0
numpy>=1.24

# Data + finance
yfinance>=0.2.36

# ML / anomaly detection
scikit-learn>=1.3

# Charts
altair>=5.0

# PDFs (RAG)
PyMuPDF>=1.23

# HTTP / API
requests>=2.31

# Excel support
openpyxl>=3.1

# Date handling (usually installed, but explicit is safer)
python-dateutil>=2.8

# Optional but useful (Streamlit sometimes pulls this indirectly)
protobuf<5
